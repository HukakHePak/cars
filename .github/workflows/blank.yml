name: CI
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:
jobs:
  check: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      
      - name: Can Build
        run: docker-compose up -d --build

      - name: Built
        run: curl http://localhost:9090
          
  build:
    needs: [check]
    runs-on: ubuntu-latest
    steps: 
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - uses: actions/checkout@master
      - name: Build Image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}:${{secrets.IMG_NAME}} .
      - name: Push image to docker.io
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}:${{secrets.IMG_NAME}}
        
      - uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          port: ${{ secrets.SERVER_PORT }}
          username: ${{ secrets.SERVER_USERNAME }}
          
          script: |
            docker pull $LOGIN/$NAME:cars
            docker run -d -p 3101:80 ${{ secrets.DOCKERHUB_USERNAME }}:${{secrets.IMG_NAME}}
      
      

